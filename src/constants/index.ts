import { STATUS } from "@/enum/common";

export const jwtDefault = {
  accessTokenSecret: "access-token-secret-example",
  accessTokenLife: "10m",
  refreshTokenSize: 100,
};

// export const UNAUTHORIZED_FAILED = {
//   message: "failed",
//   status: STATUS.UNAUTHORIZED,
// };
